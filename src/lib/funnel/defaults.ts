/**
 * Single source of truth for the live default hero headline. Used by the hero
 * (as the control/default) and shown read-only as "Control" in the experiment
 * builder so it's clear what a same-page headline test is measured against.
 */
export const DEFAULT_HERO_HEADLINE = "Get Fast, Reliable Internet — Without the Installation Headache";
